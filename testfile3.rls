//! This is just a testfile.
//! This testfile is used to check the behaviour of the parser.

//// normal comment.

/// @base <https://example.org/>.
/// @basetest <https://example.org/> .
#[whohoo(my, first, attribute)]
#[but(can, it, parse, 2)] // this attribute is only ther for testing purposes
head(?x) :- body(?x, constant).
